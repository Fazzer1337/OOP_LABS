<Window x:Class="OOP_LAB1.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:OOP_LAB1"
        mc:Ignorable="d"
        Title="To Do List" Height="600" Width="900"
        Background="#181A20"
        FontFamily="Segoe UI">

    <Window.Resources>
        <local:DateTimeToStringConverter x:Key="DateTimeToStringConverter"/>
        <local:DueTimeOverdueConverter x:Key="DueTimeOverdueConverter"/>

        <!-- Цвета и базовые стили -->
        <SolidColorBrush x:Key="PrimaryBrush" Color="#23272e"/>
        <SolidColorBrush x:Key="AccentBrush" Color="#00adb5"/>
        <SolidColorBrush x:Key="PanelBrush" Color="#222831"/>
        <SolidColorBrush x:Key="CardBrush" Color="#1f2228"/>
        <SolidColorBrush x:Key="TextBrush" Color="#eeeeee"/>
        <SolidColorBrush x:Key="SecondaryTextBrush" Color="#b5b9be"/>

        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
        </Style>

        <Style x:Key="DarkComboBoxItem" TargetType="ComboBoxItem">
            <Setter Property="Background" Value="{StaticResource PanelBrush}"/>
            <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Padding" Value="4,2"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Style.Triggers>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="{StaticResource AccentBrush}"/>
                    <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
                </Trigger>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#FF007F7F"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="DarkComboBox" TargetType="ComboBox">
            <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
            <Setter Property="Background" Value="{StaticResource PrimaryBrush}"/>
            <Setter Property="BorderBrush" Value="{StaticResource AccentBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="6,2"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="ItemContainerStyle" Value="{StaticResource DarkComboBoxItem}"/>
        </Style>

        <Style TargetType="Button">
            <Setter Property="Background" Value="{StaticResource AccentBrush}"/>
            <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Margin" Value="6"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Height" Value="34"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style TargetType="ListBox">
            <Setter Property="Background" Value="{StaticResource PanelBrush}"/>
            <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
            <Setter Property="BorderThickness" Value="0"/>
        </Style>
        <Style TargetType="ListView">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
            <Setter Property="BorderThickness" Value="0"/>
        </Style>
        <Style TargetType="TextBox">
            <Setter Property="Background" Value="{StaticResource PrimaryBrush}"/>
            <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="{StaticResource AccentBrush}"/>
            <Setter Property="Margin" Value="2"/>
            <Setter Property="Padding" Value="5"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="CaretBrush" Value="{StaticResource AccentBrush}"/>
        </Style>
        <Style TargetType="CheckBox">
            <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Левая панель: Категории -->
        <StackPanel Grid.Column="0" Background="{StaticResource PanelBrush}" Margin="0" >
            <TextBlock Text="Категории" FontWeight="Bold" FontSize="18" Margin="10 10 0 16"/>
            <ListBox x:Name="CategoriesList" Height="260" Margin="10" />
            <Button Content="Добавить категорию" Height="32" Click="AddCategory_Click"/>
            <Button Content="Удалить категорию" Height="32" Click="DeleteCategory_Click"/>
        </StackPanel>

        <!-- Центральная зона: задачи -->
        <Grid Grid.Column="1" Margin="0 0 0 0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <!-- Верхняя панель фильтров и добавления -->
            <StackPanel Orientation="Horizontal" Grid.Row="0" Margin="12 16 0 12">
                <TextBox x:Name="SearchBox" Width="200" Height="28" ToolTip="Поиск..."/>
                <Button Content="Добавить задачу" Width="140" Height="32" Click="AddTask_Click"/>
            </StackPanel>
            <!-- Список задач -->
            <ListView x:Name="TasksList" Grid.Row="2" Margin="0 12 20 10">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <Border CornerRadius="12" BorderBrush="#222831" BorderThickness="2" Margin="0 0 0 8" Padding="14"
                                Background="{StaticResource CardBrush}">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="34"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <CheckBox IsChecked="{Binding IsCompleted}" 
                                          VerticalAlignment="Top" Margin="0 6 0 0"
                                          Checked="TaskCompleted_Changed" Unchecked="TaskCompleted_Changed"/>
                                <StackPanel Grid.Column="1" Margin="12 0 0 0">
                                    <TextBlock Text="{Binding Title}" FontSize="17" FontWeight="SemiBold" />
                                    <TextBlock Text="{Binding Description}" FontSize="13" TextWrapping="Wrap" Opacity="0.6" Foreground="{StaticResource SecondaryTextBrush}"/>
                                    <WrapPanel Margin="0 8 0 0">
                                        <TextBlock Text="Приоритет:" FontWeight="Bold" Margin="0 0 3 0"/>
                                        <TextBlock Text="{Binding Priority}" Margin="4 0"/>
                                        <TextBlock Text="  |  "/>
                                        <TextBlock Text="{Binding Category}" Margin="4 0"/>
                                        <TextBlock Text="  |  "/>
                                        <TextBlock Text="{Binding DueTime, Converter={StaticResource DateTimeToStringConverter}}" Margin="4 0">
                                            <TextBlock.Style>
                                                <Style TargetType="TextBlock">
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding Converter={StaticResource DueTimeOverdueConverter}}" Value="True">
                                                            <Setter Property="Foreground" Value="Red"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </TextBlock.Style>
                                        </TextBlock>
                                    </WrapPanel>
                                </StackPanel>
                                <StackPanel Grid.Column="2" Orientation="Vertical" VerticalAlignment="Top" Margin="6 0">
                                    <Button Content="Редактировать" Tag="{Binding}" Height="28" Click="EditTask_Click"/>
                                    <Button Content="Удалить" Tag="{Binding}" Height="28" Click="DeleteTask_Click"/>
                                </StackPanel>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </Grid>
    </Grid>
</Window>
